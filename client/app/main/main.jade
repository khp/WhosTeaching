div(ng-include='"components/navbar/navbar.html"')

header#banner.hero-unit
  .container
    h1 Hmm... Which class should I take?
    p.lead Find out which professors are teaching what sections and quickly compare ratings online
    //- img(src='assets/images/UBClogo.gif', width=66, height=90 alt='UBC')
.container
  //- pre Model: {{asyncSelected | json}}
  div.spacer
  div.form-horizontal
    div.form-group
      label.col-sm-2.control-label(for='courseSearch') Search:
      div.col-xs-4
        input.form-control(type="text" id='courseSearch' ng-model="asyncSelected" placeholer="locations loaded via $http" typeahead="address for address in getCourses($viewValue)" typeahead-loading="loadingLocations" typeahead-no-results="noResults")
  i(ng-show="loadingLocations" class="glyphicon glyphicon-refresh")
  div(ng-show="noResults")
    i(class="glyphicon glyphicon-remove")
    | No Results Found

  .container
    table.table
      tr 
        td Course Code
        td Course Title
        td Professor
        td Rating
      tr(ng-repeat='subject in subjects')
        td {{subject}}
    .nav.nav-tabs.nav-stacked.col-md-4.col-lg-4.col-sm-6(ng-repeat='thing in awesomeThings')
          li
            a(href='#', tooltip='{{thing.info}}')
              | {{thing.name}}


footer.footer
  .container
    p
      | WhosTeaching@UBC?
